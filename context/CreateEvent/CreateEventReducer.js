const CreateEventReducer = (state, action) => {
  const { payload, type } = action

  switch (type) {
    case 'value':
      
      break;
  
    default:
      break;
  }
}

export default CreateEventReducer
